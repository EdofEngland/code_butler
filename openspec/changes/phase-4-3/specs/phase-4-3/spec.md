## ADDED Requirements

### Requirement: Phase 4.3 – Backend Factory & Configuration
The ai-clean project MUST fulfill the deliverables for Phase 4.3 described in docs/butlerspec_plan.md to keep the ButlerSpec roadmap on track.

#### Scenario: Phase 4.3 complete
- **GIVEN** the team is executing 4 – ButlerSpec Backend & Spec Files (Our Own Tooling)
- **WHEN** Phase 4.3 (Backend Factory & Configuration) is completed
- **THEN** the repository provides backend factory & configuration outputs covering: Implement a backend factory:; For v0, `butler` is the **only** supported backend.
- **AND** the work remains within ButlerSpec guardrails (one-plan-per-file, specs on disk, Codex executor).
