## ADDED Requirements

### Requirement: Phase 7.3 – `/apply` Command
The ai-clean project MUST fulfill the deliverables for Phase 7.3 described in docs/butlerspec_plan.md to keep the ButlerSpec roadmap on track.

#### Scenario: Phase 7.3 complete
- **GIVEN** the team is executing 7 – CLI Commands
- **WHEN** Phase 7.3 (`/apply` Command) is completed
- **THEN** the repository provides `/apply` command outputs covering: `ai-clean apply PLAN_ID`:
- **AND** the work remains within ButlerSpec guardrails (one-plan-per-file, specs on disk, Codex executor).
