## 1. Implementation
- [x] 1.1 Read backend configuration.
  - [x] 1.1.1 Add `ai_clean/spec_backends/factory.py` with `build_spec_backend(config: AiCleanConfig) -> SpecBackend`.
  - [x] 1.1.2 Inside the factory, inspect `config.spec_backend.type` (required field) and normalize the value to lowercase for comparisons.
  - [x] 1.1.3 Document supported backend names in a constant (e.g., `SUPPORTED_SPEC_BACKENDS = {"openspec": OpenSpecBackend}`) so new types can be added later.
- [x] 1.2 Instantiate and validate backends.
  - [x] 1.2.1 When the type is `"openspec"`, return an instance of `OpenSpecBackend` from `ai_clean.spec_backends.openspec`.
  - [x] 1.2.2 If the configuration references an unsupported backend, raise `ValueError` with a message listing the allowed keys.
  - [x] 1.2.3 Ensure the factory return type satisfies the `SpecBackend` Protocol by annotating the function and adding a minimal runtime smoke test (e.g., `isinstance` check or attribute presence).
- [x] 1.3 Surface configuration issues early.
  - [x] 1.3.1 Extend `ai_clean/config.py` (or a helper) to validate `spec_backend.type` during `load_config`, reusing the same supported-set constant used by the factory.
  - [x] 1.3.2 Add logging or error messages that include the path to `ai-clean.toml` so users know which file to edit when invalid values are provided.
